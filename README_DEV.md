# women俩 - 开发文档

## 项目简介

「women俩」是一个专为母女沟通设计的播客共听平台，帮助云妈与女儿在一起听播客的过程中自然展开对话，修复代际沟通的隔阂。

## 技术栈

### 前端
- HTML5 + CSS3 + JavaScript (原生)
- WebRTC (实时语音通信)
- WebSocket (实时消息同步)

### 后端
- Node.js + Express (HTTP服务)
- WebSocket (ws库) (实时通信)
- MySQL (数据存储)

### 未来扩展
- FFmpeg (音频处理)
- Whisper API (语音识别)
- ChatGPT API (内容摘要生成)

## 项目结构

```
womenlia_claude/
├── index.html              # 首页
├── player.html             # 播放页
├── review.html             # 回顾列表页
├── review-detail.html      # 回顾详情页
├── css/
│   └── style.css          # 统一样式
├── js/
│   ├── index.js           # 首页逻辑
│   ├── player.js          # 播放页逻辑
│   ├── review.js          # 回顾列表逻辑
│   └── review-detail.js   # 回顾详情逻辑
├── server/
│   ├── app.js             # 后端服务器
│   └── database.sql       # 数据库初始化脚本
├── package.json           # 项目依赖
└── env.example            # 环境变量示例

```

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `env.example` 为 `.env` 并填写配置：

```bash
cp env.example .env
```

编辑 `.env` 文件：
```
PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=womenlia
```

### 3. 初始化数据库

首先确保MySQL服务已启动，然后执行：

```bash
# 方式1: 使用npm脚本
npm run db:init

# 方式2: 手动执行
mysql -u root -p < server/database.sql
```

### 4. 启动服务器

```bash
# 开发模式（自动重启）
npm run dev

# 生产模式
npm start
```

服务器启动后访问：
- HTTP: http://localhost:3000
- WebSocket: ws://localhost:3000

### 5. 访问应用

在浏览器中打开：http://localhost:3000

## 核心功能模块

### 1. 首页 (index.html)
- 播客推荐卡片展示
- 一键发起共听邀请
- 最近共听记录查看

### 2. 播放页 (player.html)
- **随机小鸟图片展示**：每次进入随机显示鸟类图片，营造自然疗愈氛围
- **实时语音通话**：WebRTC实现母女语音交流
- **弹幕互动**：实时文字和表情弹幕飘过
- **播放同步**：双方播放进度实时同步
- **模式切换**：Casual模式（轻松）和Formal模式（正式对谈会议室）

### 3. 回顾页 (review.html)
- 显示所有共听会话历史
- 查看会话统计（时长、对话数、表情数）

### 4. 回顾详情页 (review-detail.html)
- **Shownotes**：对话要点摘要
- **对话记录回放**：语音、文字、表情完整记录
- **生成回顾播客**：将互动内容自动生成新播客
- **情感分析**：温暖度、共鸣度、理解度分析

## API接口

### WebSocket消息类型

```javascript
// 加入会话
{
  type: 'join',
  sessionId: 'session_xxx',
  podcastId: 1
}

// 发送弹幕
{
  type: 'danmaku',
  sessionId: 'session_xxx',
  content: '文字内容'
}

// 发送表情
{
  type: 'emoji',
  sessionId: 'session_xxx',
  emoji: '❤️'
}

// 同步播放进度
{
  type: 'sync',
  sessionId: 'session_xxx',
  time: 123,
  isPlaying: true
}
```

### REST API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/podcasts/recommend` | GET | 获取推荐播客列表 |
| `/api/sessions/history` | GET | 获取会话历史 |
| `/api/sessions/:id` | GET | 获取会话详情 |
| `/api/sessions/:id/generate-shownotes` | POST | 生成Shownotes |
| `/api/sessions/:id/generate-podcast` | POST | 生成回顾播客 |
| `/health` | GET | 健康检查 |

## 数据库表结构

### 核心表

- `users` - 用户表
- `podcasts` - 播客表
- `sessions` - 共听会话表
- `session_participants` - 会话参与者表
- `interactions` - 互动记录表
- `shownotes` - 对话要点表
- `emotion_analysis` - 情感分析表

详见 `server/database.sql`

## 开发注意事项

### WebRTC配置

WebRTC需要HTTPS环境或localhost才能使用。开发时使用localhost即可。

如需在局域网内测试，需要配置证书或使用STUN/TURN服务器。

### CORS配置

服务器已开启CORS，允许跨域请求。生产环境建议限制域名白名单。

### 浏览器兼容性

- Chrome/Edge 90+
- Safari 14+
- Firefox 88+

需要支持：
- WebSocket
- WebRTC
- ES6+

## 部署建议

### 1. 前端部署

可将前端文件部署到任何静态托管平台：
- Vercel
- Netlify
- GitHub Pages
- 阿里云OSS
- 腾讯云COS

### 2. 后端部署

推荐使用：
- 腾讯云服务器
- 阿里云ECS
- AWS EC2
- Heroku

### 3. 数据库

推荐使用云数据库服务：
- 阿里云RDS
- 腾讯云MySQL
- AWS RDS

### 4. 音频存储

推荐使用对象存储：
- 阿里云OSS
- 腾讯云COS
- AWS S3

## 未来功能扩展

### 短期（1-2周）
- [ ] 用户注册登录
- [ ] 播客上传功能
- [ ] 音频录制优化
- [ ] 移动端适配

### 中期（1-2月）
- [ ] AI自动生成Shownotes
- [ ] FFmpeg音频处理
- [ ] 回顾播客生成
- [ ] 推荐算法优化

### 长期（3-6月）
- [ ] 小程序版本
- [ ] 社交功能
- [ ] 数据统计看板
- [ ] 情感分析AI

## 常见问题

### Q: WebSocket连接失败？
A: 检查后端服务是否启动，端口是否被占用

### Q: 数据库连接失败？
A: 检查MySQL服务是否启动，用户名密码是否正确

### Q: 麦克风权限获取失败？
A: 确保使用HTTPS或localhost，浏览器需要授权麦克风权限

### Q: 播放页小鸟图片不显示？
A: 检查网络连接，Unsplash图片需要访问外网

## 技术支持

如有问题，请查看：
- 项目文档：`rules.md` 和 `requirements.md`
- 数据库文档：`server/database.sql`
- 代码注释：各文件内有详细注释

## 许可证

MIT License

---

**women俩团队** | 用声音连接两代女性 💕

